extern crate instant_replay;

// use instant_replay::{Request};

fn main() {
    // let reqs = Request::from_logs_file(&"test_fixtures/logs".to_string());

    // for r in reqs {
    //     println!("{:?}", r);
    // }
}
